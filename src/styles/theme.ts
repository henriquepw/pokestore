const theme = {
  colors: {
    background: '#3A234B',
    primaryText: '#FFFFFF',
    secondaryText: '#FEF003',
    active: '#07FDCC',
  },
};

export type Theme = typeof theme;
export default theme;
